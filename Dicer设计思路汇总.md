# Dicer 设计思路汇总

更新日期：2026-04-04

本文档用于说明 `Dicer` 的设计思路、模块分工与后续扩展口径。  
如果说 `README.md` 回答的是“这个项目目前做了什么”，那么本文档回答的是“为什么这样设计，以及以后应该怎么继续设计”。

本文档主要面向以下读者：

- 项目作者自己，用来整理设计口径，避免后续内容越做越散
- 后续开发者，用来理解系统边界，而不是只看表面功能
- 内容设计者，用来判断一个新角色、新装备、新法术、新敌人应该放在哪一层表达

建议阅读顺序如下：

1. 先看 `README.md`，了解当前系统现状和实现范围
2. 再看本文档，理解设计原则、职责分工和扩展方向
3. 最后看 `Dicer数据规范表.md`，按照既定口径填写具体数据

本文档优先说明以下内容：

- 核心设计目标
- 玩家与敌人的职责拆分
- 骰子、骰面与战场对象的关系
- 地图、遭遇与权重算法的设计理由
- 后续新增内容时应遵循的判断标准

本文档不负责以下内容：

- 逐项罗列当前已实现功能
- 逐字段解释 JSON 写法
- 用当前代码细节代替设计原则本身

## 0.1 当前数据工作流的设计口径

在当前项目阶段，`Dicer` 的内容编辑链路已经不是“直接手改运行时 JSON”，而是更接近双层结构：

- `CsvData/` 负责内容录入、批量改表和版本迭代
- `GameData/` 与 `Localization/` 负责运行时加载
- `tools/csv_to_json.py` / `tools/json_to_csv.py` 负责双向转换和校验

这样设计的原因，不只是为了方便录表，更是为了把“编辑层”和“运行层”分开：

- 编辑层需要更适合增量修改、对照和批量维护
- 运行层需要更稳定、结构更明确、启动时更容易直接读取
- 本地化文本需要和玩法字段分离，避免把显示文本直接写死进逻辑字段

因此后续新增内容时，默认口径应当是：

- 先改 `CsvData` 与 `localization.csv`
- 再生成运行时 JSON
- 优先使用单表转换做增量更新，减少无关文件被一起重写

这个口径和玩法设计同样有关，因为它直接影响内容扩展时的可维护性。

## 1. Dicer 的核心设计目标

`Dicer` 是一个围绕“骰面构筑”展开的游戏。  
玩家不是只在战斗中掷骰，而是在局外和局内共同决定：哪些内容会出现在骰子上、哪些内容会在本局成为主要资源、哪些内容会通过投掷结果转化成节奏和效果。

当前设计中，有几个核心目标需要长期保持：

- 投掷结果应当有朝向差异，而不是只看是否成功
- 投掷应当保留随机感，但短期结果不能长期明显背离当前骰面组合的期望分布
- 倒数应当是核心节奏资源，而不是纯装饰性数值
- 构筑应当来自骰面组合，而不是只靠统一的角色升级或数值堆叠
- 新增内容最好能影响“角色、装备、法术、倒数、朝向”中的至少一个层面

也就是说，`Dicer` 的重点不是让每个对象都做所有事，而是让不同系统分别掌管不同资源，然后通过投掷把它们串起来。

## 2. 玩家侧三类核心单位的职责分工

玩家侧三类核心单位分别是 `角色`、`装备`、`法术`。  
这三者不是同层竞争关系，而是同一套战斗系统中的三个职责层。

### 2.1 角色

角色负责生命和承接状态相关的交互，资源是倒数。

角色是战斗中的行动主体，也是大部分“谁在承受结果”的落点。  
生命、护盾、状态、死亡、行动顺序、倒数归零后的结算，这些都应当优先挂在角色这一层。
如果要做“亡语 / 死亡时触发 / 被击杀时触发”这类设计，推荐统一挂到角色事件 `last_breath`，不要再分散成各类临时事件名。

以后设计新角色时，优先考虑的不是“再做一个伤害数值不同的人”，而是：

- 倒数节奏有什么差异
- 更适合承接什么状态或风险
- 作为行动主体时，会把什么样的法术和装备组合放大

### 2.2 装备

装备负责管理战斗修正，资源是骰子状态，尤其是“是否投出”和“是否正面朝上”。

装备本身不是主要行动主体，而是对战斗结算施加结构性修正。  
这里的“加成”不是狭义的面板攻击和防御，而是更广义的攻击修正与战斗修正，包括但不限于：

- 直接攻击加值
- 伤害倍率修正
- 蓄力后的集中爆发
- 额外伤害形成的伪多段
- 面朝上时的被动
- 失去正面时的变化

这些内容本质上都属于“本次行动如何被修正”，因此都更适合由装备负责。

以后设计新装备时，优先考虑的不是“单纯加更大数值”，而是：

- 它依赖什么骰子状态触发
- 它如何改变当前回合的攻击修正结构和防御修正结构
- 它是否通过正面、反面、失去正面的过程带来节奏变化

### 2.3 法术

法术负责输出效果，资源是投掷行为和倒数。

法术是主要的效果表达层。  
伤害、治疗、状态施加、目标选择、特殊效果，原则上优先由法术承担。法术不直接管理生命系统本身，也不主要负责攻击修正或战斗修正结构，而是把“这次行动具体做了什么”表达出来。

以后设计新法术时，优先考虑的是：

- 它在什么投掷结果下发动
- 它如何和倒数节奏联动
- 它改变的是输出方式、目标关系，还是状态分布

### 2.4 为什么要这样分工

这样分工的目的，是避免职责重叠。

如果角色、装备、法术都同时负责生命、加成、效果、节奏，那么后续新增内容时就很容易出现：

- 一个新设计不知道该放在哪一层
- 同一种效果在多个系统里重复实现
- 平衡和可读性都越来越差

当前口径下，可以把三者理解为：

- 角色负责“谁行动、谁承伤、谁挂状态”
- 装备负责“本次行动以什么方式被修正”
- 法术负责“这次行动具体产生什么效果”

这个边界不是绝对禁止交叉，而是要求交叉时必须有明确理由。

## 3. 玩家骰子与骰面的设计思路

`Dicer` 里必须先分清“骰子”和“骰面”不是一回事。

骰子是资源容器，决定一个系统以什么形式被投掷和轮转。  
骰面是内容载体，决定具体有哪些角色、装备、法术可以出现在这个容器中。

### 3.1 骰子是资源容器，骰面是内容载体

骰子描述的是一套可被反复投掷的结构。  
骰面描述的是这套结构里每一个位置可能承载的具体内容。

这意味着设计一个新内容时，需要先判断：

- 它是应该作为“某个系统中的一个面”
- 还是应该作为“决定这一整类资源如何运作的规则”

### 3.2 角色骰

角色骰决定谁在场、谁承接状态、谁消耗倒数行动。

它不是简单的角色列表，而是当前战斗中“角色引用关系”的入口。  
角色骰当前面为空、为谁朝上、如何在未填面时重定向，都会影响后续谁被当作当前角色来参与逻辑。

### 3.3 装备骰

装备骰决定本回合的战斗修正，以及面朝上相关触发。

它更像是一层战斗修正器。  
同一件装备作为骰面被嵌入后，真正产生影响的时机通常来自投掷后的状态变化，而不是它静态存在于库存里。

这里的战斗修正同样应按广义理解，包括攻击倍率、蓄力层数、爆发阈值、追加伤害、伪多段、以及其他通过骰子状态改变本次行动结算方式的内容。

### 3.4 法术骰

法术骰决定当前可释放的效果集合。

法术不是常驻光环，而是围绕当前投掷结果和倒数节奏进入结算。  
因此法术设计应当优先强调“触发条件”和“效果表达”，而不是被动面板属性。

### 3.5 触发思路

当前系统里，投掷不是单一动作，而是一个战斗节奏节点。

投掷之后，系统会产生至少几层意义：

- 当前骰子的朝向结果被更新
- 相关的 face-up 或 roll 触发有机会结算
- `global_tick` 推进双方角色的倒数
- 倒数归零的角色进入行动结算

`global_tick` 的意义，在于把“投出一次骰子”和“战场时间推进一次”绑定在一起。  
这样做可以保证玩家对投掷次数、投掷顺序和拖回合行为都要承担节奏后果。

倒数归零之所以是行动闸门，是因为它把构筑和行动频率连接起来。  
谁更快行动、谁能更早吃到某种状态、谁能在法术与装备联动下抢先结算，都通过倒数体现。

这里还需要一个固定口径：  
若某个单位已经因为这次 `global_tick` 进入行动队列，那么在同一序列里后续才施加给它的 `slow` / `speedup`，不应回溯改写它这次已经锁定的行动。  
这类倒数改变量应当顺延到它本次行动重置倒数之后再生效，也就是直接改写它“下一次行动的起始倒数”。  
同理，这类状态也不应在目标本次已排队行动的 `after_action` 中立刻衰减，否则玩家会感受到“状态挂上了，但节奏上什么都没改变”。

面朝上的重要性在不同系统里并不相同：

- 对装备和法术来说，面朝上通常直接影响它们是否能生效或怎样生效
- 对角色来说，面朝上更多影响“当前引用的是谁”，而不是单纯的被动开关

#### 3.5.0 关于投掷概率修正的口径

当前战斗中的掷骰逻辑，设计上采用“保留随机感的期望回归”而不是纯均匀随机，也不是严格发牌袋。

这里的固定口径如下：

- 概率修正按“结果类别”生效，而不是按具体面位生效
- 相同内容的重复面应视为同一类结果，和它们在 6 个槽位中的排列顺序无关
- 空白面也是合法类别，应统一作为同一种结果参与期望计算
- 修正只针对当前可投候选面工作；若某些面因状态或规则被排除，本次投掷的期望分布也应随候选集一起变化
- 修正的目标是减少短期极端偏离，而不是消灭随机波动本身

这样设计的原因是：

- 玩家感知的是“这个结果怎么老不出”，而不是“第 3 格怎么老不出”
- `1:1`、`3:2:1`、带空白面的骰子，本质上都只是“类别权重不同”的同一问题
- 如果直接按面索引补偿，会把“同内容重复面”错误地拆成多个独立结果，违背构筑含义

因此后续如果继续扩展投掷相关机制，默认应遵循：

- 先判断它影响的是“候选面集合”
- 还是影响“类别间的期望分布”
- 不要把“内容类别”和“面位索引”混为一谈

#### 3.5.1 关于 `flip` 的结算口径增补

当前口径中，`flip` 被视为一次可以定向改写当前投掷结果的强操作。  
因此，只要某个效果成功执行了 `flip`，它就应继续沿用一次完整投掷结果更新的后续结算，包括：

- 当前骰子的 face-up 结果更新
- 相关 face-up / roll / 亮出类触发重新检查
- 一次 `global_tick`
- 由这次 `global_tick` 带出的倒数推进、倒数归零行动与后续连锁

这条口径的目的，是把“改结果”也纳入战斗节奏成本。  
如果未来某个设计希望只改朝向、不承担节奏推进代价，那么它不应复用当前 `flip` 语义，而应另立一种更弱的结果修正机制。

#### 3.5.2 当前已确认的放大点

在保留上述口径后，以下联动会被一并放大，这是当前设计上接受的结果：

- `flip equipment` 会重新触发装备 face-up 切换相关结算
- `flip hex` 会重新进入 Hex 投掷结果链，因此可能再次触发 `swift`
- 任意 `flip` 只要走完整结果更新，就会推进一次 `global_tick`
- 因 `global_tick` 推进产生的倒数变化，仍会继续触发依赖倒数变化的敌方机制和逃跑计时机制

当前检查结论是：`fh014` 作用于 `enemy_frien`，主要额外成本是再推进一次 `global_tick`；它不会直接进入玩家 `frien` 的 `deploy / reinforce` 这条链。  
因此现阶段不需要为 `slap` 单独拆分新口径，只需要在设计上明确它属于强控制型 Hex。

### 3.6 为什么允许空白面和未填面存在

空白面不是纯粹的临时缺口，而是构筑自由度的一部分。

如果强制所有骰子必须填满，那么玩家实际上只能做“有什么就全塞进去”的选择。  
允许未填面存在以后，玩家可以主动承担稳定性下降，换取更明确的构筑倾向；也可以保留某些空面，让系统中的重定向、空转和风险控制本身成为设计的一部分。

因此空白面应当被视为一种合法状态，而不是失败状态。

### 3.7 战斗内查看层的口径

战斗内新增的骰面展开查看层，设计目标不是替代定制界面，而是补充“当前战斗运行时状态”的可读性。

这个查看层应长期遵循以下口径：

- 它是只读信息层，不承担任何嵌入、替换、移除或定制职责
- 它展示的是运行时骰面状态，而不是战斗开始时的构筑快照
- 它应直接反映减员、清面、补面、朝上面变化等战斗内结果
- 它保留固定的六面结构，避免因为减员或清面导致展示布局跳动

这样设计的原因是，战斗内玩家真正需要确认的是“这颗骰子现在还能掷出什么”，而不是“我最初给它装了什么”。

因此在角色阵亡后，对应角色面即使被运行时清空，查看层也不应隐藏这些格子，而应保留空白/失效态。  
这能让玩家一眼看出：

- 当前骰子剩余的有效面有多少
- 哪些面已经因为战斗过程失效
- 当前战场信息与局外构筑之间的差异

## 4. 战场实例和骰面的区别

这一章的目的是避免后续设计“只影响单个骰面”的效果时出现概念混淆。

在 `Dicer` 中，至少要区分三层对象：

### 4.1 数据层的骰面定义

这是库存中的一个角色、装备或法术条目，是可被嵌入的内容模板。

它回答的问题是：

- 这个面本来是什么
- 它有哪些静态属性
- 它在构筑阶段能否被放进某类骰子

这一层是模板层，不是战斗中的具体对象。

### 4.2 骰子上的槽位结果

这是某个骰子当前朝上的一个面，是一次投掷后的局部结果。

它回答的问题是：

- 这次投掷之后，当前哪一个槽位朝上
- 当前战斗正在引用哪个面来结算
- 这个面的朝向状态有没有变化

这一层强调的是“当前结果”，不是长期存在的独立单位。

### 4.3 战场实例

这是进入战斗后实际存在、可被选中、可承接状态和效果的运行时对象。

它回答的问题是：

- 这个对象现在是否在场
- 它当前有多少生命、护盾和状态
- 它是不是这次效果真正作用到的目标

战场实例是运行时层。  
同一个模板进入不同战斗，可以生成不同实例；同名对象在同一战斗里也必须被视为不同实例。

### 4.4 为什么必须区分这三层

如果不区分，就会在后续设计里出现一系列问题：

- 同一个效果不知道是改模板、改当前结果，还是改正在战斗中的对象
- 多个同名敌人或同模板对象无法区分目标
- 一次性效果和持续性效果会混淆到一起

“影响某个模板”和“影响当前这个实例”是两种完全不同的设计。  
前者更接近构筑层调整，后者更接近战斗层结算。

### 4.5 后续单骰面效应的口径

以后如果设计“对单个骰面的效果”，必须先声明它绑定的是哪一层对象。

推荐口径如下：

- 如果效果针对“当前朝上的那个面”，它作用的是当前战斗中的面结果或面实例
- 如果效果针对“某类可构筑内容”，它作用的是数据定义或构筑层槽位
- 如果效果会持续到后续投掷、翻面、替换或战斗结束，必须先说明它绑定的是实例、槽位还是内容模板

### 4.6 这一章带来的设计约束

以后所有“对单个骰面的效果”，都必须先说明目标对象到底是：

- 模板
- 槽位
- 当前面实例

如果这一点说不清，规则设计和运行时实现都会混乱。

## 4.7 数据模板、本地化文本与运行时对象也必须分层

除了“模板 / 槽位 / 实例”这三层，当前项目还必须额外区分：

- 数据模板字段
- 本地化 key
- 运行时显示文本

原因很简单：一个角色、装备、Hex、状态或 Trait，在运行时看到的文字，并不应该天然等于它的数据主键或内部逻辑字段。

当前更合理的口径是：

- 稳定引用靠 `id`
- 显示文本靠 `display_name_key / description_key / title_key`
- 实际展示内容由 `Localization/zh_CN.json` 与 `Localization/en.json` 提供

这样做有几个直接收益：

- 改文案时，不必改逻辑字段
- 做双语或后续扩语言时，不需要重做整套数据结构
- 历史存档、状态匹配、Trait 触发条件等逻辑，不会因为中文名或英文名变化而失效

这也是为什么当前项目里应尽量避免：

- 用本地化后的名称反查状态或 Trait
- 把显示文案直接当作逻辑 key
- 为了改 UI 文案去重命名真实数据主键

## 5. 敌人的设计思路

敌人不是独立于玩家规则之外的怪物模板，而是被遭遇系统打包、被战斗初始化展开的对手构筑。

从设计上看，敌人的强度不应只来自“单体数值高不高”，还应来自它在遭遇中的组合方式、倒数压力、装备池与法术池的搭配。

设计敌人时，至少要考虑以下几个维度：

- 基础生命与倒数
- 敌方装备池
- 敌方法术池
- 遭遇中的数量组合
- 个别敌人的特殊机制

这意味着敌人的设计单位并不总是“单个 enemy 条目”，很多时候真正被玩家感受到的单位其实是“这一组遭遇”。

## 6. 敌我同源的设计思路

`Dicer` 的一个重要方向，是敌我同源。

这里的同源，不是要求玩家和敌人完全共用同一套表现形式，而是要求他们尽量围绕同一批底层资源运作：

- 生命
- 倒数
- 装备
- 法术
- 状态

### 6.1 同源点

敌我双方都不是纯数值单位，而是围绕战斗节奏运转的对象。  
敌人也有生命、倒数、装备池、法术池，说明它们不是完全脱离玩家规则的“脚本怪”。

### 6.2 不同点

玩家通过库存和定制显式构筑自己的骰子。  
敌人则通过敌人数据和遭遇组合，在战斗初始化阶段隐式构筑。

玩家的重点是选择与规划。  
敌人的重点是压力与展示。

### 6.3 设计收益

敌我同源有几个明显收益：

- 规则一致，玩家更容易理解敌人的威胁来源
- 扩展便宜，很多战斗资源可以共用同一套概念
- 平衡更容易比较，因为双方都落在生命、倒数、装备、法术这些共同坐标上

### 6.4 敌我骰子的异同点

相同点在于：

- 都有角色位、装备位、法术位对应的战斗职能
- 都会受到投掷、朝向、倒数和状态的影响

不同点在于：

- 玩家骰面来自库存与定制
- 敌方骰面来自敌人数据与遭遇拼装
- 玩家承担构筑选择，敌人承担关卡压力与机制表达

## 7. 地图生成与房间分布思路

地图系统不只是把房间随机摆出来，而是通过路径结构塑造选择。

地图层的核心目标有三个：

- 让玩家面对路径分叉而不是单线推进
- 让死路具备价值，而不是纯惩罚
- 让 Boss 作为地图最远目标，形成自然终点感

### 7.1 房间生成逻辑

当前地图采用从起点向外扩张的方式生成。  
生成时会控制总房间数以及死路数量，保证地图既不会过于稀疏，也不会变成没有决策感的网格。

当前实现还额外固定了两个边界：

- 地图始终保持无环树结构，不通过补边制造回路
- 房间分叉目标按楼层写死到生成器配置中，而不是完全交给随机结果

这意味着地图系统当前追求的不是“任意拓扑都可能出现”，而是“在随机感内维持明确的层级节奏”。

当前楼层拓扑口径如下：

- 第 1 层：非前台不允许出现 `T` 形或十字路口
- 第 2 层：至少出现 1 个非前台 `T` 形节点
- 第 3 层：至少出现 1 个非前台十字路口节点

这样设计的理由是：

- 第 1 层更像教学层，路径应保持清晰，避免过早把读图压力和绕路判断叠给玩家
- 第 2 层开始引入“真正分叉点”，让玩家开始面对路径价值差异
- 第 3 层再引入更强的中枢节点，让地图选择、敌人投放和特殊房取舍都有更明显的空间表达

当前实现不是简单地“随机到符合条件为止”，而是采用两阶段生成：

1. 先生成满足该层目标的保底骨架
2. 再随机扩张补足该层目标房间数

这个口径的好处是，楼层表达由设计先决定，再由随机过程补充变化，而不是让设计目标被随机结果吞掉。

当前房间数与死路控制范围为：

- 第 1 层：`10` 房间，`3~4` 条死路
- 第 2 层：`15` 房间，`4~5` 条死路
- 第 3 层：`19` 房间，`5~7` 条死路
- 第 4 层：`22` 房间，`5~6` 条死路

这种做法的重点不在“随机”，而在“生成可用于选择的结构”。

### 7.2 特殊房间放置逻辑

当前设计中，特殊房间的放置思路应保持明确理由：

- Boss 放在最长死路末端，是为了让地图天然有终点感
- Pantry 靠近起点，是为了让恢复点更像中程规划节点，而不是最终补给
- Shop 和 HR 优先占死路，是为了让绕路成为主动选择，而不是主线顺手路过

同时，当前实现增加了一个明确保护规则：

- 非前台 `T` 形和十字路口会被记录为保留分叉节点
- `Boss / Shop / HR / Pantry` 都不应占用这些保留分叉节点
- `Pantry` 额外限制为不能放在 `neighbor_count >= 3` 的分叉节点上

这样做的目的，是防止“设计上要求出现的空间结构”被特殊房间覆盖掉，导致分叉拓扑虽然生成了，但普通战斗空间反而被抹平。

特殊房间不应只是图标差异，而应当通过位置本身产生决策价值。

### 7.3 普通房间主题

普通房间当前区分为 `OFFICE` 和 `CORRIDOR` 等主题。  
这类主题既承担一定表现职责，也承接战斗内容入口。

后续设计应尽量保持“房间主题”和“房间内容”可部分解耦。  
这样将来可以调整视觉和关卡表达，而不必同时重写整个玩法结构。

在当前地图口径下，还需要额外记住一点：

- 房间主题不等于房间拓扑职责
- `OFFICE / CORRIDOR` 主要负责视觉和内容入口
- “枝干 / L 形 / T 形 / 十字路口”属于空间结构分类，应当由节点度数和连接方向判断

如果后续要把敌人、事件或掉落和房间结构绑定，应优先绑定到拓扑结构，而不是直接绑定到主题名。

## 8. 遭遇与权重算法的设计理由

遭遇系统的重点不是让每个敌人都精确计算复杂战力，而是先用一个简单、透明、可维护的方式控制整体压力。

### 8.1 单个敌人的 `weight` 表示什么

`weight` 不是美术体量，也不是单纯生命值。  
它更像是一个敌人对整场遭遇压力的抽象估值。

这个估值应综合反映：

- 生存能力
- 行动频率
- 输出和控制威胁
- 与其他敌人组合时的大致压力

### 8.2 遭遇难度如何计算

当前遭遇难度的核心思路是：多个敌人的 `weight` 直接求和。

这个做法的优点是：

- 简单
- 透明
- 快速
- 便于扩内容

在内容量还不算特别大时，这种粗粒度方式比复杂预算系统更容易维护。

### 8.3 楼层如何筛选 normal / elite

当前设计中，不同楼层会根据总权重区间筛选普通战和精英战。  
这样做的本质，是先把“遭遇是什么级别的压力”定义清楚，再去决定它出现在什么房间中。

### 8.4 为什么当前算法适合项目现阶段

它适合当前项目，是因为它满足几个现实需求：

- 内容扩张速度要快
- 调整成本要低
- 新增敌人时不需要先接入复杂模拟器
- 设计者可以直接用相对直观的方式理解“这组敌人大概多强”

### 8.5 这个算法的局限

当前算法也有明确局限：

- 它不能准确表达机制联动强度
- 它对控制、逃跑、召唤类敌人的压力估值可能失真
- 当内容量增大后，可能需要加入额外标签、角色分类或更复杂的预算规则

因此，`weight` 应被视为当前阶段的实用抽象，而不是最终答案。

## 9. 后续新增内容的设计口径

这部分是本文档最重要的落地用途：以后新增内容时，应该先判断它在系统中改变的到底是什么。

### 9.1 新角色

新角色应优先新增倒数节奏差异和状态承接差异。  
不要优先把角色做成“只是数字更大或更小的另一个壳”。

### 9.2 新装备

新装备应优先新增投出后状态变化、正面朝上触发差异，或者战斗修正结构差异。  
装备最适合表达“当前这次投掷如何修正战斗”。

### 9.3 新法术

新法术应优先新增输出方式、目标选择和倒数互动。  
法术最适合表达“当前这次行动具体造成什么结果”。

### 9.4 新敌人

新敌人应优先体现构筑压力，而不只是单纯把数值拉高。  
设计时要同时看单体表现和遭遇组合后的战场形态。

### 9.5 新房间

新房间应优先提供路径决策价值，而不是只多一个功能按钮。  
房间如果不能改变玩家是否愿意绕路、是否愿意承担风险、是否愿意延后收益，那么它对地图结构的贡献就会比较弱。

### 9.6 新增内容时应先问自己的问题

以后新增任意内容时，建议先判断以下问题：

- 这个设计主要在改谁的资源
- 它是在改变节奏、改变构筑，还是改变结算
- 它是否和现有三类核心单位职责冲突
- 它作用的是战场实例、当前面实例，还是构筑层内容模板

如果这些问题答不清，就说明这个设计还没有真正落到系统里。

## 9.7 UTF-8 与文本稳定性的设计约束

当前项目里，文本不是附属问题，而是内容系统的一部分。

已经确定的约束是：

- CSV、JSON、文档、存档统一按 `UTF-8` 处理
- CSV 导入允许 `UTF-8 BOM`，运行时 JSON 与存档统一按 `UTF-8` 输出
- 中文文本、英文文本和逻辑字段必须允许稳定共存

这条约束的意义在于：

- 避免中文策划表、说明文档和运行时资源之间出现编码不一致
- 避免“看起来只是文案修改”实际破坏数据读取或存档兼容
- 保证本地化文本可以独立迭代，而不会污染核心逻辑字段

以后只要新增的是“可见文本”，就应优先判断它属于：

- 逻辑主键
- 本地化 key
- 某个语言下的显示内容

这三者如果混在一起，后续维护成本会迅速上升。

## 10. 当前设计的边界与待验证问题

本文档不是把所有问题都定死，而是把当前已经确认的边界先固定下来。

目前已经确认的口径包括：

- 角色负责生命和承接状态相关的交互，资源是倒数
- 装备负责管理战斗修正，资源是骰子状态，尤其是投出和正面朝上
- 法术负责输出效果，资源是投掷行为和倒数
- 战场实例、骰面模板、当前朝上的面需要明确区分
- 敌我同源是长期设计方向

目前仍然需要继续验证的问题包括：

- 当前权重是否足以覆盖更复杂的敌群组合
- 单骰面持续效果未来更适合绑定到实例、槽位，还是模板
- Boss 是否继续沿用精简敌人结构，还是要升级成独立构筑系统
- 地图房间未来是否需要在权重、事件、关卡表达之间建立更强的联动
- `flip` 作为强结果修正时，是否会让特定装备 face-up 触发或 `swift` Hex 的收益过高

后续如果设计方向发生变化，应优先更新本文档中的设计口径，再去更新具体数据和代码。

## 11. 文档使用建议

为了让这份文档长期可维护，后续继续补充时建议保持统一写法：

- 先说明这部分回答什么问题
- 再说明当前设计口径
- 然后解释为什么这样设计
- 最后补充以后新增内容时如何沿用这个口径

建议避免以下写法：

- 把 `README.md` 的功能清单原样复制进来
- 把数据字段表直接粘进本文档
- 用“当前代码刚好怎么写”替代“设计原则为什么成立”
- 只写结论，不写设计理由

这份文档的价值，不在于完整罗列现状，而在于帮助后续开发者在新增内容时保持同一套判断标准。

