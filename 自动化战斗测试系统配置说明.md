# 自动化战斗测试系统配置说明

本文档说明项目内自动化战斗测试系统的配置方式。核心入口涉及：

- `Scenes/battle_simulation_runner.tscn`
- `Scripts/dev/battle_simulation_runner.gd`
- `GameData/battle_simulation_config.json`
- `Scripts/database/BattleManager.gd`

## 1. 系统执行链路

自动化战斗测试的执行流程如下：

1. 启动 `battle_simulation_runner.tscn`。
2. `battle_simulation_runner.gd` 加载 JSON 配置文件。
3. runner 根据配置展开敌人列表、构建测试用 `battle_config`。
4. runner 调用 `GameDataManager.set_battle_test_config(battle_config)`。
5. runner 实例化 `Scenes/battle_scene.tscn`。
6. `battle_scene.gd` 在 `_ready()` 中读取测试配置，并调用 `BattleManager.start_test_battle(cfg)`。
7. runner 按固定顺序自动掷骰，直到 `BattleManager.battle_result` 变成 `victory` 或 `defeat`。
8. 战斗结束后，runner 从 `BattleManager.get_last_battle_summary()` 读取摘要，并输出 JSON/CSV。

固定掷骰顺序为：

- `frien`
- `equipment`
- `hex2`
- `enemy_frien`
- `enemy_equipment`
- `enemy_hex2`

## 2. 默认配置文件

默认配置文件路径：

```json
res://GameData/battle_simulation_config.json
```

当前示例内容：

```json
{
	"runs": 3,
	"seed": 1001,
	"fast_mode": true,
	"output_path": "user://battle_simulation_results.json",
	"player_setup": {
		"player_character_ids": ["fc001", "fc002", "fc003"],
		"player_equipment_ids": ["eq001", "eq002", "eq004"],
		"player_hex_ids": ["fh001", "fh002", "fh012"],
		"embedded_faces": {},
		"reroll_count": 0
	},
	"encounter_setup": {
		"mode": "encounter_list",
		"encounter_ids": ["enc010"],
		"label": "sample_enc010"
	}
}
```

## 3. 顶层配置字段

### `runs`

- 类型：`int`
- 含义：连续模拟战斗次数。
- 默认值：`1`
- 行为：最小值被强制为 `1`。

### `seed`

- 类型：`int`
- 含义：基础随机种子。
- 行为：第 `n` 次战斗实际种子为 `seed + (n - 1)`。

例如：

- 第 1 场：`1001`
- 第 2 场：`1002`
- 第 3 场：`1003`

### `fast_mode`

- 类型：`bool`
- 含义：是否开启快速模拟。
- 默认值：`false`
- 效果：
  - 战斗布局动画时间归零。
  - 血量队列间隔归零。
  - 法术表现请求会被跳过。
  - 更适合批量跑数值测试。

### `output_path`

- 类型：`string`
- 含义：结果 JSON 输出路径。
- 默认值：`user://battle_simulation_results.json`
- 行为：
  - runner 会写出一个 JSON。
  - 同时自动生成同名 CSV，路径为 `output_path` 去掉扩展名后加 `.csv`。

例如：

- `user://battle_simulation_results.json`
- `user://battle_simulation_results.csv`

### `battle_type`

- 类型：`int`
- 含义：战斗类型，最终传给 `BattleManager.current_battle_type`。
- 默认值：`BattleManager.BattleType.BATTLE`
- 当前枚举值：
  - `0`：普通战斗 `BATTLE`
  - `1`：精英战斗 `ELITE_BATTLE`
  - `2`：Boss 战 `BOSS_BATTLE`

## 4. `player_setup` 配置

`player_setup` 用于生成玩家侧测试战斗配置。

### `player_character_ids`

- 类型：`string[]`
- 含义：玩家角色 ID 列表。
- 来源：`GameDataManager.get_character_data(id)`
- 行为：
  - runner 会把这些角色展开成嵌入骰面。
  - 如果数量少于 6，会循环填充到 6 个面。
  - 无效 ID 会被跳过。

### `player_equipment_ids`

- 类型：`string[]`
- 含义：玩家装备 ID 列表。
- 来源：`GameDataManager.get_equipment_data(id)`
- 行为与角色相同，也是循环填满 6 面。

### `player_hex_ids`

- 类型：`string[]`
- 含义：玩家 Hex ID 列表。
- 来源：`GameDataManager.get_hex_data(id)`
- 行为与角色相同，也是循环填满 6 面。

### `embedded_faces`

- 类型：`Dictionary`
- 含义：对自动生成的玩家骰面配置做递归覆盖。
- 默认值：`{}`

这是最容易写错的字段。这里的键名不是 `hex2`，而是 `EmbeddedDiceFaces.from_dict()` 使用的序列化键名：

- `frien`
- `equipment`
- `hex`

示例：

```json
{
	"player_setup": {
		"player_character_ids": ["fc001"],
		"player_equipment_ids": ["eq001"],
		"player_hex_ids": ["fh001"],
		"embedded_faces": {
			"frien": {
				"0": {
					"id": "fc002"
				}
			},
			"hex": {
				"3": {
					"id": "fh012"
				}
			}
		}
	}
}
```

说明：

- runner 先根据 `player_*_ids` 生成基础 6 面。
- 再把 `embedded_faces` 递归 merge 到生成结果上。
- 也就是说，`embedded_faces` 适合做局部覆盖，不适合完全代替基础配置。

### `reroll_count`

- 类型：`int`
- 含义：测试战斗初始重掷次数。
- 最终落到 `BattleManager.test_reroll_count`。

## 5. `encounter_setup` 配置

当前 runner 只支持两种模式：

- `enemy_entries`
- `encounter_list`

### 模式一：`enemy_entries`

适合直接指定敌人列表。

示例：

```json
{
	"encounter_setup": {
		"mode": "enemy_entries",
		"encounter_id": "manual_case_01",
		"label": "manual_case_01",
		"enemies": [
			"en001",
			{
				"enemy_template_id": "en002",
				"unique_id": "elite_en002_a",
				"new_max_health": 40,
				"current_health": 40,
				"hexes": ["fh020", "fh021"]
			}
		]
	}
}
```

其中 `enemies` 数组每个元素支持两种写法。

#### 写法 A：字符串

```json
"en001"
```

表示直接按敌人或 Boss 模板 ID 生成一个敌人。

#### 写法 B：字典

支持字段：

- `enemy_template_id`
  或 `enemy_id`
  含义：敌人模板 ID，runner 最终通过它去 `GameDataManager.get_enemy_data()` 或 `get_boss_data()` 取模板。
- `unique_id`
  含义：运行时唯一 ID。
- `new_max_health`
  含义：覆盖敌人最大生命值，同时初始化时也会用于敌人基础血量。
- `current_health`
  含义：覆盖敌人当前生命值。
- `hexes`
  含义：覆盖敌人 Hex 列表。
- `timed_escape_state`
  含义：覆盖敌人的限时逃跑状态。

如果没有显式提供 `unique_id`，系统会按敌人名自动补唯一后缀。

下面补几个常见例子。

##### 例 1：使用 `enemy_id` 作为模板字段别名

```json
{
	"encounter_setup": {
		"mode": "enemy_entries",
		"encounter_id": "manual_case_alias",
		"label": "manual_case_alias",
		"enemies": [
			{
				"enemy_id": "en001",
				"unique_id": "alias_en001_a"
			}
		]
	}
}
```

说明：

- `enemy_id` 和 `enemy_template_id` 二选一即可。
- 这个例子适合你只想手动指定运行时唯一 ID，不改其他属性。

##### 例 2：只覆盖生命值，不写 `unique_id`

```json
{
	"encounter_setup": {
		"mode": "enemy_entries",
		"encounter_id": "manual_case_hp_only",
		"label": "manual_case_hp_only",
		"enemies": [
			{
				"enemy_template_id": "en002",
				"new_max_health": 60,
				"current_health": 18
			}
		]
	}
}
```

说明：

- `new_max_health` 会覆盖最大生命值。
- `current_health` 会覆盖当前生命值。
- 不写 `unique_id` 时，系统会自动生成一个运行时 ID。

##### 例 3：覆盖敌人的 Hex 组合

```json
{
	"encounter_setup": {
		"mode": "enemy_entries",
		"encounter_id": "manual_case_hex_override",
		"label": "manual_case_hex_override",
		"enemies": [
			{
				"enemy_template_id": "en003",
				"unique_id": "hex_test_en003_a",
				"hexes": ["fh020", "fh021", "fh022"]
			}
		]
	}
}
```

说明：

- `hexes` 会直接覆盖模板里的 Hex 列表。
- 适合验证某个敌人在特定法术组合下的行为。

##### 例 4：手动注入 `timed_escape_state`

```json
{
	"encounter_setup": {
		"mode": "enemy_entries",
		"encounter_id": "manual_case_timed_escape",
		"label": "manual_case_timed_escape",
		"enemies": [
			{
				"enemy_template_id": "fb003",
				"unique_id": "escape_fb003_a",
				"timed_escape_state": {
					"countdown_mode": "tick",
					"remaining_ticks": 2,
					"remaining_escapes": 1,
					"escapes_done": 0,
					"max_health_bonus_per_escape": 3,
					"max_health_bonus_accumulated": 0,
					"marker_id": "pipeling",
					"escape_status_id": "",
					"escape_status_duration": 0,
					"upgraded_hexes_after_escape": ["fh030"],
					"transit": false
				}
			}
		]
	}
}
```

说明：

- `timed_escape_state` 会作为运行时状态写入敌人数据。
- 适合复现限时逃跑、逃跑后强化等战斗逻辑。
- 如果只想用敌人模板里的默认限时逃跑配置，可以不写这个字段。

##### 例 5：字符串写法和字典写法混用

```json
{
	"encounter_setup": {
		"mode": "enemy_entries",
		"encounter_id": "manual_case_mixed",
		"label": "manual_case_mixed",
		"enemies": [
			"en001",
			{
				"enemy_template_id": "en001",
				"unique_id": "en001_elite_variant",
				"new_max_health": 45,
				"current_health": 45,
				"hexes": ["fh020", "fh025"]
			},
			"en003"
		]
	}
}
```

说明：

- 同一个 `enemies` 数组里可以同时混用两种写法。
- 适合快速拼一个“默认敌人 + 定制敌人”的测试场景。

### 模式二：`encounter_list`

适合按遭遇配置批量展开敌人。

示例：

```json
{
	"encounter_setup": {
		"mode": "encounter_list",
		"encounter_ids": ["enc010", "enc011"],
		"label": "enc010_plus_enc011"
	}
}
```

行为说明：

- runner 会读取每个 `encounter_id` 对应的 `enemies` 数组。
- 多个 encounter 会被直接拼接成一个总敌人列表。
- `encounter_id` 会在结果里被记录为用逗号拼接后的字符串。

可选字段：

- `encounter_source_path`
  含义：覆盖默认遭遇源文件。
  要求：该 JSON 顶层需要有 `encounters` 字段。

如果不写 `encounter_source_path`，默认使用 `GameDataManager.encounters`，而它当前来自：

```json
res://GameData/encounters_test.json
```

## 6. runner 生成的运行时战斗配置

源配置不会直接原样传给 `BattleManager`。runner 会组装出一个运行时配置，主要字段如下：

```json
{
	"is_test_mode": true,
	"is_simulation_mode": true,
	"fast_mode": true,
	"battle_type": 0,
	"reroll_count": 0,
	"player_character_ids": [],
	"player_equipment_ids": [],
	"player_hex_ids": [],
	"embedded_faces": {},
	"enemies": [],
	"persist_on_exit": false,
	"simulation_context": {
		"battle_index": 1,
		"seed": 1001,
		"encounter_id": "enc010",
		"enemy_config_label": "sample_enc010",
		"enemy_entries": []
	}
}
```

几个关键点：

- `is_simulation_mode` 会让 `BattleManager` 在结算后不跳转 `battle_result.tscn`。
- `persist_on_exit` 当前被固定写死为 `false`。
- `simulation_context` 会被写入统计摘要，用于区分每一轮结果。

## 7. 输出结果结构

### JSON 输出

JSON 顶层结构：

```json
{
	"runs": 3,
	"seed": 1001,
	"config_snapshot": {},
	"results": []
}
```

字段说明：

- `runs`：总战斗次数。
- `seed`：基础随机种子。
- `config_snapshot`：完整配置快照。
- `results`：每场战斗的摘要数组。

### 单场结果字段

`BattleManager` 当前构建的战斗摘要包含以下核心字段：

- `battle_index`
- `seed`
- `encounter_id`
- `enemy_config_label`
- `enemy_entries`
- `result`
- `total_ticks`
- `player_total_casts`
- `player_damage_dealt_by_unit`
- `player_damage_taken_by_unit`
- `player_healing_received_by_unit`
- `player_shield_received_by_unit`
- `enemy_healing_received_total`
- `enemy_shield_received_total`

其中：

- `result` 目前只有 `victory` 或 `defeat`。
- 各 `*_by_unit` 字段是字典，key 为运行时单位 ID，value 为累计值。

### CSV 输出

当前 CSV 会固定输出以下列：

- `battle_index`
- `result`
- `total_ticks`
- `player_total_casts`
- `enemy_config_label`
- `encounter_id`
- `enemy_healing_received_total`
- `enemy_shield_received_total`
- `player_damage_dealt_by_unit`
- `player_damage_taken_by_unit`
- `player_healing_received_by_unit`
- `player_shield_received_by_unit`

字典或数组字段会被序列化成 JSON 字符串后写入单元格。

## 8. 启动方式

### 方式一：在编辑器中直接运行场景

直接运行：

```text
Scenes/battle_simulation_runner.tscn
```

它会自动读取默认配置：

```text
res://GameData/battle_simulation_config.json
```

### 方式二：命令行运行

可用 Godot headless 方式执行，例如：

```powershell
godot4 --headless --path "d:\Godot Projects\dicer" --scene "res://Scenes/battle_simulation_runner.tscn"
```

如果要指定其他配置文件，可追加用户参数：

```powershell
godot4 --headless --path "d:\Godot Projects\dicer" --scene "res://Scenes/battle_simulation_runner.tscn" -- --config=res://GameData/battle_simulation_config.json
```

runner 的参数解析规则：

- 优先识别 `--config=...`
- 如果用户参数里出现任意 `.json` 路径，也会直接拿它当配置路径
- 否则回退到默认配置文件

## 9. 与 `BattleManager` 的关系

`BattleManager` 对自动化战斗测试的关键支持点如下：

- `start_test_battle(config)`
  负责进入测试模式、读取 `fast_mode`、读取 `simulation_context`、初始化敌我状态。
- `_initialize_battle_stats(config)`
  初始化当前战斗统计对象。
- `check_battle_end()`
  在战斗结束时写入 `battle_result` 和 `last_battle_summary`。
- `_finalize_battle_end_if_needed()`
  在模拟模式下只发 `battle_simulation_completed` 信号，不切场景。
- `get_last_battle_summary()`
  提供 runner 回收单场结果。

## 10. 注意事项

### 1. `embedded_faces` 使用的是 `hex`，不是 `hex2`

这是当前实现的序列化约定。写成 `hex2` 不会被 `EmbeddedDiceFaces.from_dict()` 读取。

### 2. `encounter_list` 读取的是遭遇数据里的 `enemies`

它不会读取别的扩展字段。最终拼出来的只是敌人条目数组。

### 3. `runs` 会让种子自动递增

如果你想复现某一场具体结果，需要记录该场实际 `seed`，不是只看基础 `seed`。

### 4. `fast_mode` 适合批量统计，不适合观察表现层

因为它会去掉大量时间延迟和法术表现请求。

### 5. 输出路径日志中会转成绝对路径

runner 在打印结果时会输出 `ProjectSettings.globalize_path()` 之后的路径，便于直接定位文件。

## 11. 建议用法

如果目标是做稳定批量测试，建议：

- 固定 `seed`
- 把 `runs` 设为需要的样本数
- 开启 `fast_mode`
- 用 `encounter_list` 绑定固定 encounter
- 用 `embedded_faces` 只覆盖你真正要控制的个别骰面

如果目标是做极端 case 验证，建议：

- 使用 `enemy_entries`
- 直接传字典型敌人条目
- 显式指定 `unique_id`、`new_max_health`、`current_health`、`hexes`
